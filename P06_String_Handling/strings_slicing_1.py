# reate a program called strings_slicing_1 that first inputs a string.
# Then the program should print the following: first two characters in the string and every other character in the string.
# NB! You should use slicing in this exercise.

user_input = input("Enter a string: ")

print(user_input[0:2])
print(user_input[0::2])
